package theater.domain;

public class Title {
    private String title;

    public boolean isSame(Title screeningTitle){
        if(title.equals(screeningTitle.ge)){
            return true;
        }
        return false;
    }
}
